import styled from 'styled-components'
import { BaseDialogContent, BaseDialogOverlay } from '../../styles/global'

export const EditDialogOverlay = styled(BaseDialogOverlay)``

export const DialogContent = styled(BaseDialogContent)``
